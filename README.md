#Simple web app in Go that goes over:
  - using "net/http" package to build web apps
  - using "html/template" package to process HTML templates
  - using the "regexp" package to validate input
  - closures
